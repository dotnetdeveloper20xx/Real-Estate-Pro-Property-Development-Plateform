import { Routes } from '@angular/router';

/**
 * Global Search feature routes.
 * The search is a global overlay accessible from any page via Ctrl+K,
 * so no dedicated routed pages are needed. This file exists as a placeholder
 * for any future search-related routes (e.g., advanced search page).
 */
export const GLOBAL_SEARCH_ROUTES: Routes = [];
